# Secrets / env
.env
.env.*
*.env

# Dependencies
node_modules/

# Build output
dist/
tsconfig.tsbuildinfo

# Editor / OS
.DS_Store
.vscode/

# Local tooling / agent state (but keep the shared Figma adapter in git)
.claude/*
!.claude/figma-adapter.md
.superpowers/

# Local asset backups (originals kept locally, not in repo)
.logo-orig-backup/